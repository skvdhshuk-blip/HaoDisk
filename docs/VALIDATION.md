# 验证记录

最新候选版：0.2.1 (3)，文件夹清理回归见 [FOLDER_CLEANUP_FIX.md](FOLDER_CLEANUP_FIX.md)。0.2 商业化布局与真实目录验收见 [COMMERCIAL_ACCEPTANCE.md](COMMERCIAL_ACCEPTANCE.md)。

## 0.1 基线

日期：2026-09-06。环境：Apple Silicon Mac、macOS 26.4、Xcode 26.6、Swift 6.3.3。最低部署版本为 macOS 14。

## 已验证

| 层面 | 结果 |
| --- | --- |
| Swift Package 核心测试 | 15 项通过，0 失败 |
| Xcode 原生 XCTest | 15 项通过，0 失败 |
| Debug 构建 | 通过 |
| Release 构建 | 通过，包含 arm64 / x86_64 两种架构 |
| 本地开发证书签名 | `codesign --verify --strict` 通过，Hardened Runtime 开启 |
| Release 权限 | 只有 App Sandbox、用户选择读写、应用授权书签；无 `get-task-allow` |
| 资源 | App Icon 各尺寸正确；Info、隐私清单与 entitlements 的 plist 检查通过 |
| 系统目录授权 | 从 NSOpenPanel 选择隔离样本目录，成功扫描并显示结果 |
| 重启恢复 | 退出并重新启动，点击继续上次分析，成功解析书签与重新扫描 |
| 目录 / 面积图 | 样本 Movies、Projects、Downloads 等目录大小与占比对应，下钻和表格模式可用 |
| 受保护项目 | Library 选中后显示保护原因，加入清理按钮不可用 |
| 权限错误 | 对自建不可读目录显示 1 处未完整读取，不把缺失结果当成成功扫描 |
| 清理流程 | 自建测试文本文件加入清单、确认后成功移到系统废纸篓；原路径消失，废纸篓同名项存在；App 自动重扫，项目数从 18 降至 17 |

废纸篓正文被 macOS 拒绝直接读取，因此没有声称进行废纸篓内容的字节比对，也没有清空或修改用户既有的废纸篓内容。

Xcode 仅输出不影响构建的 App Intents 元数据提示（未使用 AppIntents.framework），没有 Swift 编译警告。截图为原生 App 的实际运行结果，扫描对象为专门创建的样本目录。

## 自动测试覆盖

1. 嵌套、隐藏和零字节文件的统计与父级汇总。
2. 符号链接循环不跟随，硬链接只计一次。
3. 稀疏文件的逻辑大小与已分配大小不同。
4. 取消与项目上限不会返回完整扫描状态。
5. 面积比例正确、边界不越界、矩形不重叠；零 / 无效权重被忽略。
6. 路径前缀碰撞、大小写与外置卷系统路径保护。
7. 清理清单中的父子重叠去重。
8. 根目录、Library 及包含 Library 的父目录不能清理。
9. 文件变化、父目录被替换为链接、深层内容变化时拒绝清理。
10. 系统废纸篓调用失败时保留错误，不降级永久删除。
11. 无权限目录被记录为不完整，其清理被阻止。

## 仍需发布前验证

- macOS 14 / 15 与 Intel 真机运行；当前 Intel 仅证明构建成功。
- App Store 分发归档、Validate App、TestFlight 和正式审核。
- iCloud / 第三方 File Provider、外置或只读文件系统、网络卷与超过当前 12,000 直接子项规模的文件树。
- VoiceOver 完整流程、提高对比度 / 减少透明度等辅助功能设置。深色外观已在 0.2 本机完成验收。
- 文件访问授权被系统撤销、卷离线、跨版本迁移等长期使用情景。

扫描是某一时刻的观察。清理前会再次核对路径身份和文件夹内容，但路径式系统废纸篓 API 与其他进程之间不提供全局原子事务；正在被其他程序持续修改的目录应停止写入后再处理。APFS 共享块与快照也使“已统计大小”无法等同于“清空后实际可释放空间”。
